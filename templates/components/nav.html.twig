<!--Nav-->
<nav id="header" class="fixed w-full z-30 top-0 pt-8 text-white lg:pr-10 lg:pl-10">
    <div class="w-full container mx-auto flex flex-wrap items-center justify-between mt-0 py-2">
        <div class="pl-4">
            <a class="flex items-center toggleColour text-white no-underline hover:no-underline font-bold text-2xl lg:text-4xl" href="{{ path('home') }}">
                <img id="logo-blanc" class="h-10 w-10 mr-4" src="{{ asset('build/images/svg/dés.svg') }}" alt="logo"/>
                <img id="logo-noir" class="h-10 w-10 mr-4 u-display-none" src="{{ asset('build/images/svg/dés-noir.svg') }}" alt="logo"/>
                WILO
            </a>
        </div>
        <div class="block lg:hidden pr-4">
            <button id="nav-toggle" class="flex items-center p-1 text-pink-800 hover:text-gray-900 focus:outline-none focus:shadow-outline transform transition hover:scale-105 duration-300 ease-in-out">
                <svg class="fill-current h-6 w-6" viewbox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                    <title>Menu</title>
                    <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/>
                </svg>
            </button>
        </div>
        <div class="w-full flex-grow lg:flex lg:items-center lg:w-auto hidden mt-2 lg:mt-0 bg-white lg:bg-transparent text-black p-4 lg:p-0 z-20" id="nav-content">
            <ul class="list-reset lg:flex justify-end flex-1 items-center">
                <li class="mr-3">
                    <a class="inline-block py-2 px-4 text-black {% if app.request.get('_route') == 'home' %}font-bold{% endif %} no-underline" href="{{path('home')}}">ACCUEIL</a>
                </li>
                <li class="mr-3">
                    <a class="inline-block text-black no-underline hover:text-gray-800 hover:text-underline py-2 px-4 {% if app.request.get('_route') == 'games' %}font-bold{% endif %}" href="{{path('games')}}">JEUX</a>
                </li>
                <li class="mr-3">
                    <a class="inline-block text-black no-underline hover:text-gray-800 hover:text-underline py-2 px-4 {% if app.request.get('_route') == 'contact' %}font-bold{% endif %}" href="{{path('contact')}}">CONTACT</a>
                </li>
            </ul>
            {% if app.user != null %}
                <div class='bg-white flex rounded-lg shadow'>
                    <div class="avatar flex items-center pl-4">
                        <div class="w-24 image-cropper">
                            <img class="h-24 w-auto img-crop" src="https://api.lorem.space/image/face?hash=47449">
                        </div>
                    </div>
                    <div class='flex flex-col p-4 justify-evenly'>
                        <span class='mb-2'>{{app.user.email}}</span>
                        <span class='mb-2'>{{app.user.jetons}} Jetons</span>
                        <a href="{{path('app_logout')}}">Déconnexion</a>
                    </div>
                </div>
            {% else %}
                <a href="{{path('login')}}">
                    <button id="navAction" class="mx-auto lg:mx-0 bg-white text-black font-bold rounded-full mt-4 lg:mt-0 py-4 px-8 shadow opacity-75 focus:outline-none focus:shadow-outline transform transition hover:scale-105 duration-300 ease-in-out">
                        Connexion
                    </button>
                </a>
            {% endif %}
        </div>
    </div>
</nav>